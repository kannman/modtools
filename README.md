# modtools
```
Usage:
  modtools [command]

Available Commands:
  help        Help about any command
  path        get path for current version of module
  vendor      copy files matching glob pattern to ./vendor (**/*.c **/*.h **/*.proto)

Flags:
  -h, --help      help for modtools
  -v, --verbose   verbose output
```
